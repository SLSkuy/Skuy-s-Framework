# Skuy's Framework

Listen-server 多人玩法的领域语言：房间是聚会与名册，对局才有模拟世界。房主从菜单直接开战；加入方进入的是已经在打的房间。

## Language

**进程**：
整段应用寿命。`GameCore` 登记的框架服务属于进程，不属于某一房间。
_Avoid_: 全局会话, 壳生命周期（口语可以，文档用进程）

**会话**：
从本机建房或加入被接受，到回到菜单。本进程的 `BattleManager` 与为该房启动的网络属于会话。
_Avoid_: 战局, 对局（对局是会话里可开关的一段）, 大厅（本产品没有等人大厅）

**菜单**：
无房间时的流程态，入口场景是 `MainScene`。
_Avoid_: 大厅

**房间**：
跨对局意图存活的聚会与名册。本进程对象是 `BattleRoom`。房主一建房就开战，房间仍在；本阶段结束对局即解散房间。
_Avoid_: 战局, 大厅, lobby（指房间对象时）

**对局**：
房主离开菜单、提交进关之后的那段（不必等关卡加载或 spawn 完成）。只有对局才允许模拟核、玩家 pawn 与 Tick。对局进行中仍可入座。
_Avoid_: 战局, 房间, 局内（口语可以，文档用对局）

**关卡**：
对局发生的场景，现为 `GameScene`。只承载几何与同步视野，不是第二套进程壳。
_Avoid_: 游戏场景当启动器, 第二个 GameCore（历史残留）

**房主**：
名册上的主持玩家，且本机作为 listen-server 持有 `NetServer`。房主离开则解散房间。不做房主迁移。
_Avoid_: 服务器, dedicated, 主机（与 Unity Host 混用时以房主为准）

**加入方**：
本进程会话角色为 Client：持有 `NetClient`，名册权威在房主进程。加入发生时对局通常已经开始。
_Avoid_: 客户, 账号, Client（指人时）

**玩家**：
房间名册上的一位成员，身份是房主分配的 `playerId`，独立于传输连接。玩家 pawn 的 `entityId` 等于 `playerId`。
_Avoid_: 连接, clientId（传输的不是玩家）

**连接**：
传输层会话，键为 `connectionId`。连接可以对应一名玩家，但不是玩家。
_Avoid_: 玩家, peer（文档用连接）

**名册**：
房间内当前玩家集合。加入必须写入双方名册。对局进行中可以入座；离开则除名。不做准备就绪、房间码、发现、重连、房主迁移。
_Avoid_: 大厅列表（那是 UI）

**本机房**：
容量为 1、不接受远端加入的房间。与多人房同一条路径：建房后立即开战并进入关卡。
_Avoid_: 单机旁路, 无房间本地玩
