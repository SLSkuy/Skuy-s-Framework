# Skuy's Framework

Listen-server 多人玩法的领域语言：没有聚会房间，也没有夹在中间的会话。离开菜单就是对局，对局一开始就可以加入；房主退出对局则解散，所有人回菜单。

## Language

**进程**：
整段应用寿命。框架服务属于进程，不属于某一对局。
_Avoid_: 全局会话, 壳生命周期（口语可以，文档用进程）

**菜单**：
无对局时的流程态，入口场景是 `MainScene`。点了加入但尚未被接受时，仍在菜单；此时可以有一条尚未入座的连接，但没有对局。菜单不加载关卡。
_Avoid_: 大厅, 加入中（作为流程态）, 会话

**对局**：
房主离开菜单之后（加入方则是被接受之后）直到房主退出并回到菜单。名册、开听与入座属于对局，与玩法编排分开。没有「一场」、没有再来一局；开始即接加入。不必等关卡加载或 spawn 完成。只有对局才允许模拟核、玩家 pawn 与 Tick。
_Avoid_: 战局, 房间, 一场, 会话, 局内（口语可以，文档用对局）, 大厅, lobby

**本机玩**：
不接远端加入的对局。与接远端的对局同一条路径，不是另一种玩法。
_Avoid_: 单机旁路, 本机房, 无房间本地玩

**关卡**：
对局发生的场景，现为 `GameScene`。只承载几何与同步视野，不是第二套进程壳。
_Avoid_: 游戏场景当启动器, 第二个 GameCore（历史残留）

**关卡控制**：
对局里负责何时加载与切换关卡（进度、事件、最短展示）。不是玩法粘合点，也不管名册和连接。场景 IO 由 `SceneLoader` 负责。切场景时先清空实例门面，再让资源门面卸载。
_Avoid_: 菜单流程, 大厅, 资源策略兼做关卡流程

**资源门面**：
进程级资源入口，持有当前资源策略。只负责加载与卸载，不实例化 GameObject，不持有预制体实例池。
_Avoid_: 资源管理系统, 二次缓存, 资源系统兼做实例化

**实例门面**：
进程级入口：从资源位置生成与回收 GameObject。内部按资源位置池化；加载仍走资源门面。调用方不直接对资源句柄 Instantiate，也不 Dispose 预制体句柄。某资源位置借出为零后，按进程级全局超时销毁空闲实例并释放句柄。相机等预制体同样走资源位置，不靠 ScriptableObject 索引场景里已有对象。
_Avoid_: 资源管理系统, ResourceManager.Instantiate, PoolManager（指预制体实例池）

**资源策略**：
实现了资源加载接口的模块。负责加载、卸载与句柄生命周期。门面不代替它做缓存。实例化不属于资源策略。
_Avoid_: Loader（旧 `IResourceLoader` 只做 IO）, 资源加载器（口语可以，文档用资源策略）

**资源位置**：
一次加载请求的身份：`location` 必填，`package` 可空。`package` 只给策略做路由，不是热更/下载 API。类型名为 `AssetLocation`。
_Avoid_: path 与 key 混称, 资源路径（口语可以，文档用资源位置）

**资源句柄**：
一次加载的所有权凭证，由 YooAsset 的 `AssetHandle` 表达加载状态与释放。门面与策略只转发，不二次封装。场景加载不在本期资源门面范围内。
_Avoid_: GetAsset, ManagedCache, ResourceHandle（旧类型）, CancellationToken（资源加载）

**房主**：
名册上的主持玩家，且本机作为 listen-server 开听。房主退出对局则解散。不做房主迁移。
_Avoid_: 服务器, dedicated, 主机（与 Unity Host 混用时以房主为准）

**加入方**：
本进程作为 Client 连入已经开始的对局。名册权威在房主进程。未被接受前仍在菜单。加入方离开只除名，对局继续。
_Avoid_: 客户, 账号, Client（指人时）

**玩家**：
名册上的一位成员，身份是房主分配的 `playerId`，独立于传输连接。玩家 pawn 的 `entityId` 等于 `playerId`。
_Avoid_: 连接, clientId（传输的不是玩家）

**连接**：
传输层上的一条链路，键为 `connectionId`。连接可以对应一名玩家，但不是玩家。
_Avoid_: 玩家, peer（文档用连接）

**名册**：
对局里当前玩家集合。加入必须写入双方名册。对局进行中可以入座；加入方离开则除名；房主退出则解散。不做准备就绪、房间码、发现、重连、房主迁移。
_Avoid_: 大厅列表（那是 UI）, 房间, 会话
