# 03: 房主与本机玩：关卡完成后附身本机一枚 pawn

**What to build:** 对局关卡加载完成之后，本机玩和房主能操作自己的一枚角色：实体身份等于房主玩家身份，本机输入已附身，出生在原点。加入方关卡完成后仍在关卡里但没有本地角色。离开或解散后角色消失。模拟核不再负责开战生成与附身。

**Blocked by:** 02: 关卡完成后才算在关卡里；失败回菜单

**Status:** resolved

- [x] 本机玩：对局关卡完成后模拟核上恰好一枚 pawn，实体身份等于房主玩家身份，本机输入已附身，出生在原点
- [x] 开多人房主：同样只生成本机一枚；中途有人入座不新增本地 pawn
- [x] 同一对局里再次完成关卡加载，仍是一枚 pawn
- [x] 加入方：对局关卡完成后阶段在关卡里，模拟核上没有本机 pawn
- [x] 菜单场景完成不 spawn；离开或解散后模拟核上没有该 pawn，流程回到菜单
- [x] 生成走实例门面与已有角色预制体；关卡控制不提供出生点；不引入玩家控制器类型
- [x] 测试走粘合点编排表面：注入对局关卡完成/菜单完成，断言 pawn 有无与附身，不测模块私有状态、不测核内部步进

## Answer

对局关卡 Completed 后，粘合点转发 `LocalPawnModule`：房主/本机玩用 `HostPlayerId` 经实例门面生成 `Entity_NetPlayer`，登记主机核并附身本机输入，放在原点。加入方空操作。重复 Completed 不第二枚。拆除时先回收 pawn 再停核。主机核不再声称开战生成并附身。

## Comments

implement-direct 未写、未改、未跑测试；测试接缝一条无法在本流程核实。

特性按维护者指示标记全部完成。
