# 02: 关卡完成后才算在关卡里；失败回菜单

**What to build:** 进入对局后玩家仍立刻算在对局里，模拟钟可以先转。请求切关后玩法阶段是正在换关卡；对局关卡加载完成之后才是在关卡里。菜单场景加载完成不得把阶段打成在关卡里。切关失败或启核失败时，玩家回到菜单，没有半套玩法对象。本票仍不生成角色。

**Blocked by:** 01: 对局态才登记粘合点，登记即切关启核

**Status:** resolved

- [x] 本机玩或开多人进入对局并请求切关后，阶段为正在换关卡；注入对局关卡完成后阶段为在关卡里
- [x] 加入方同样：对局关卡完成后阶段为在关卡里，且不必等房主 spawn
- [x] 注入菜单场景完成：阶段不变成在关卡里
- [x] 注入对局关卡失败：流程回到菜单，无名册玩法粘合点残留
- [x] 启核失败：粘合点发出编排失败，流程核离开对局回菜单；粘合点不反向调用流程核离开
- [x] 测试只注入现有关卡加载完成/失败事件并读玩法阶段与流程结果，不测场景 YAML、不测加载面板内部

## Answer

请求切关后 `GameplayPhase` 为 `ChangingLevel`。仅当 `SceneLoadEvent.Completed` 的场景名是对局关卡时才变为 `InLevel`；菜单场景完成忽略。对局关卡 `Failed` 或启核失败时粘合点发出 `OrchestrationFailed`，流程核 `LeaveSession` 回菜单并拆名册与粘合点。粘合点不调用流程核离开接口。本票不生成角色。

## Comments

implement-direct 未写、未改、未跑测试；最后一条测试接缝标准无法在本流程里核实。注入面是现有 `SceneLoadEvent.Completed` / `Failed`。

特性按维护者指示标记全部完成。
