# 03: 失败换态；去掉编排失败事件与关卡相位

**What to build:** 切关失败或模拟核启动失败时，玩家直接回到菜单，没有半套玩法对象、也没有名册。失败不再绕编排失败事件；对局态把流程切回菜单，拆除发生在离开对局态时。不再有关卡相位：玩家仍只看见菜单与对局，有没有关卡问关卡控制或当前场景。菜单场景加载完成仍不能把人留在假对局或误 spawn。

**Blocked by:** 01: 对局态接线，删除粘合类型

**Status:** resolved

- [x] 注入对局关卡失败：流程为菜单，无名册，无关卡控制，无模拟核，无本机 pawn
- [x] 启核失败：同样回到菜单且无残留；对局态不调用流程核的离开接口
- [x] 不再存在编排失败事件；失败就是流程切到菜单
- [x] 不再存在关卡相位；测试不读相位，只读流程态、登记结果与 pawn
- [x] 注入菜单场景完成：不 spawn，流程不变成「因菜单加载而失败回菜单」以外的异常态
- [x] 测试走流程核公开意图并注入现有关卡失败事件，不测场景 YAML、不测加载面板内部

## Answer

对局关卡 Failed 或启核失败时，对局态 `ChangeState(菜单)`，不调用流程核离开接口。已删除编排失败事件与关卡相位枚举。菜单场景 Completed 不 spawn。

## Comments

与 01 同一提交落地。MCP 自动跑测在进播放模式时会话中断，需在 Test Runner 里进播放后重跑。

维护者验收通过。
